{
  "id": "43chat-openclaw-plugin",
  "channels": [
    "43chat-openclaw-plugin"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "channelConfigs": {
    "43chat-openclaw-plugin": {
      "label": "43Chat",
      "description": "43Chat inbound notifications for the OpenClaw main session.",
      "schema": {
        "type": "object",
        "additionalProperties": true,
        "properties": {
          "enabled": { "type": "boolean", "default": true },
          "baseUrl": { "type": "string", "format": "uri", "default": "https://43chat.cn" },
          "apiKey": { "type": "string" },
          "requestTimeoutMs": { "type": "integer", "minimum": 1000 },
          "sseReconnectDelayMs": { "type": "integer", "minimum": 100 },
          "sseMaxReconnectDelayMs": { "type": "integer", "minimum": 1000 },
          "sseHeartbeatTimeoutMs": { "type": "integer", "minimum": 1000 },
          "accounts": {
            "type": "object",
            "additionalProperties": {
              "type": "object",
              "additionalProperties": true,
              "properties": {
                "enabled": { "type": "boolean", "default": true },
                "name": { "type": "string" },
                "baseUrl": { "type": "string", "format": "uri", "default": "https://43chat.cn" },
                "apiKey": { "type": "string" },
                "requestTimeoutMs": { "type": "integer", "minimum": 1000 },
                "sseReconnectDelayMs": { "type": "integer", "minimum": 100 },
                "sseMaxReconnectDelayMs": { "type": "integer", "minimum": 1000 },
                "sseHeartbeatTimeoutMs": { "type": "integer", "minimum": 1000 }
              }
            }
          }
        }
      }
    }
  }
}
